# 项目协作规则

* 默认使用中文沟通，除非用户明确要求使用其他语言。
* 用户明确要求构建或打包前，先完成代码修改并等待确认；不要擅自打包。
* 产品简介里的当前版本格式固定为 `v1.2.0 - YYYY-MM-DD HH:mm`。
* 每次修改应用代码后，必须同步更新 `src/App.vue` 中的 `versionLabel`，时间取本次代码修改完成时间。
* 打包前必须检查 `versionLabel` 是否已更新到最新应用代码修改时间。

## 自动化测试协作规则

* 自动化测试分为“本次变更测试”和“主场景回归测试”；测试分层、目录与命令以 `docs/TESTING_STRATEGY.md` 为准。
* 每次 Agent 完成项目代码变更后，如用户无特殊要求，必须根据需求、改动文件和受影响业务路径判定测试范围，新增或更新本次变更测试用例，并执行对应的 `test:change:*` 命令。
* 本次变更测试失败时，必须先分析并调整实现或测试用例；仅在测试反映的需求已满足后再次执行。循环至通过后方可交付本次代码变更；若无法继续，必须报告失败结果和阻塞原因。
* 每次自动化测试执行完成后，必须在对话中逐条报告本次实际执行的用例名称及其通过、失败或跳过状态；失败用例还必须附带简要错误原因。不得只报告汇总数量。
* 执行打包、发布或远端更新（包括提交、推送、创建发布）前，如用户无特殊要求，必须评估本次改动是否影响稳定核心用户路径；需要时更新主场景回归测试用例，并执行 `npm run test:main`。
* 主场景回归测试未通过时，必须停止打包、发布和远端更新流程，向用户报告失败结果；不得以测试失败状态继续这些操作。
* 全量验证使用 `npm test`。发布前除主场景回归外，还必须执行本次变更对应的测试；`npm test` 只有在已配置的全部变更测试入口均已纳入时才能替代上述组合执行。
* 前端测试必须 Mock Tauri `invoke`，后端测试必须使用内存 SQLite 或等效隔离环境；禁止自动化测试访问、修改或清理真实用户数据库。

## 云服务版改造协作规则

* 云服务版的完整产品与技术方案以 `docs/CLOUD_SERVICE_UPGRADE_PLAN.md` 为准；涉及账号、订阅、同步、云端数据或跨端客户端的改造前，必须先遵守该文档。
* 保持本地优先：本地 SQLite 是桌面客户端可离线工作的真实数据源。所有业务写入必须与待同步操作（`sync_outbox`）在同一事务中完成；不得让联网成功成为本地操作成功的前提。
* 基础本地功能永久免费、无需登录且可完全离线使用。云端同步是订阅解锁的可选服务；订阅到期、网络不可用或云服务异常时，不得限制用户读取、编辑或管理本地数据。
* 云端身份与本地用户名必须分离：不得把现有 `owner` 本地昵称作为云端账号、鉴权依据或跨设备主键。服务端只能从令牌解析账户与授权空间。
* 跨端同步实体必须使用稳定 UUID；不得使用 SQLite 自增 ID 作为节点、事件或同步操作的跨设备标识。同步通信传递版本化操作，不传递 SQLite 文件、SQL 或整表覆盖数据。
* 同步必须支持断网补偿、幂等重试、增量游标、删除墓碑和冲突记录。服务端超时或无响应时，待同步操作必须保留，只有收到确认后才可标记完成。
* 任务和节点采用字段级合并；新增节点与生命周期/进度事件采用追加合并；删除使用墓碑防止数据复活。无法安全自动裁决的冲突必须保留记录并提供后续处理入口，禁止静默丢失数据。
* 自动同步应在启动、网络恢复、应用恢复、本地变更及定时检查时后台触发；默认减少打扰，仅对重新登录、长期积压、本地写入失败和待人工处理冲突作明显提示。
* 云端服务、用户 Web、管理后台和移动端保持独立项目：云端 API 为唯一服务端权威，用户 Web 包含官网与登录后服务门户，管理后台仅供内部运营，移动端独立维护。
* 当前 Windows 客户端继续在现有 MIT 开源仓库维护；云服务实现、管理后台、密钥、支付及内部运营资料必须放在私有项目中。公开客户端不得提交任何生产密钥、数据库连接、支付验签密钥、管理接口或内部运营信息。
* 云端 API 契约以云服务项目的版本化 OpenAPI 为唯一权威来源。客户端仅依赖已发布的兼容 API 版本；接口演进优先新增可选字段，不兼容变更必须升级主版本并保留迁移策略。
* 上线真实云账户、支付或云数据前，必须准备并审核云同步服务协议、云端隐私政策、账户注销与数据删除规则，以及数据安全说明；云服务应由公司主体承担责任。现有本地版 MIT 许可证继续保留。
