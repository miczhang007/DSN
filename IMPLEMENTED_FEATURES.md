# 桌面便签 / StickyNote - 已实现功能需求文档

## 1. 产品定位

面向 Windows 桌面的轻量本地待办便签工具。用户可在桌面上维护多个用户的待办事项，记录截止时间、紧急标记与执行进度。

- 当前应用展示版本：`v1.4.1 - 2026-09-01 14:10`
- 数据默认保存在本机 SQLite 数据库，不上传服务器。
- 支持 Microsoft Store MSIX 与便携 EXE 分发。

## 2. 任务管理

### 2.1 任务状态模型

当前任务状态分为五类：

- 未开始：未来任务，未到达开始执行时间。
- 待完成：新建任务（非未来任务）与周期任务实例的默认状态。
- 进行中：待完成任务点击“进行中”后进入；未来任务到达开始执行时间后自动进入。
- 已完成：各状态任务均可标记完成，完成后保留在当前任务列表划线展示。
- 已挂起：待完成/进行中任务可挂起；激活后变更为待完成。

已归档任务细分为已完成、未完成；删除为软删除状态（从历史任务中删除后进入）。

状态由 `completed_at / archived_at / deleted_at / suspended_at / start_at / started_at / recurring_setting_id` 字段派生，并在任务数据中随接口返回。

### 2.2 待办列表

- 展示当前用户的未归档任务（含未开始、待完成、已挂起、已完成未归档任务）。
- 列表不展示状态标签；进行中任务标题前展示绿色圆点；周期任务展示“周期任务”标签；已挂起任务样式为灰色；已完成任务以划线方式保留在当前列表，待手工归档或系统隔天自动归档后移入历史任务列表。
- 画圈按钮为完成主操作（点击后确认完成时间）。
- 挂起、激活、进行中、撤销完成、归档等操作集中在任务详情页操作栏。
- 排序规则：手工排序优先；其次紧急任务优先；随后按最早未完成节点计划时间升序；无节点任务靠后。
- 支持鼠标按住任务行拖拽进行手工排序，拖拽后顺序持久化保存。
- 任务卡展示任务标题、下一节点信息与紧急标识；未来任务展示开始执行时间。
- 下一节点计划时间已过时，展示“已延迟”标识。
- 空列表时提示用户添加首个任务。

### 2.3 创建任务

- 支持填写任务标题，最大 80 字。
- 支持设置可选紧急标记。
- 支持勾选“未来任务”标签：自动添加“任务开始执行”节点，可维护任务开始执行日期；未到达开始执行时间时任务为“未开始”，到达后自动变更为“进行中”。
- 支持可选填写首个节点名称（最大 40 字）与计划日期，作为任务的首个节点。
- 新建任务（除未来任务）默认为待完成状态。
- 标题为空时不可提交。
- 创建任务时自动写入生命周期记录。

### 2.4 编辑、完成与操作

- 支持编辑任务标题、紧急标记与未来任务开始执行日期。
- 对任务内容、紧急状态、开始执行时间的每次实际变更均会写入生命周期记录。
- 任务操作（详情页操作栏，按状态显示）：
  - 挂起：适用于待完成与进行中任务（未开始任务不可挂起）。
  - 激活：适用于已挂起任务，激活后变更为待完成状态。
  - 进行中：适用于待完成任务，变更为进行中状态。
  - 已完成：适用于其他各状态任务；完成后保留在当前任务列表划线展示，不立即归档。
  - 撤销完成：适用于已完成的当前任务。
  - 归档：适用于各状态任务；已完成任务归档免确认并保留已完成状态。
- 系统隔天自动归档前一天已完成任务；已归档未完成任务可补记完成。
- 历史任务撤销完成时询问是否同时撤销归档：选择“撤销完成并撤销归档”才重新加入当前任务列表，选择“仅撤销完成”则保留归档状态。

### 2.5 周期任务

- 创建任务时可选择“一次性任务”或“周期任务”。
- 周期任务可配置长期或指定日期范围、每日/每工作日/指定每周几，以及每日生成时间（默认 06:00）。
- 系统在应用运行时自动补齐应生成的当日任务，名称附带任务日期；任务实例默认为待完成状态，列表页同时展示“周期任务”标签。
- 跨日未完成实例自动归档为“未完成”，历史任务中可切换完成状态；仅当日实例撤销完成后会回到待办。
- 菜单提供“周期任务设置”，可查看状态、编辑后续生成规则和查看该设置已创建的任务；更新不追溯历史实例。

### 2.6 节点管理

- 任务支持维护多个节点，节点由名称和计划日期组成；计划日期支持仅选择日期（日期与可选时间同一行，时间默认不指定）。
- 节点维护直接在任务详情页进行，独立于任务编辑与进度录入（展开任务编辑或添加进度时不显示节点区）：支持添加节点、编辑节点名称与计划日期、删除节点。
- 节点行各操作按钮默认收起，点击节点行展开操作：未完成节点提供 编辑/删除；已完成节点提供 撤销完成/删除。
- 未完成节点行首提供完成入口（点击后确认完成时间）；已完成的节点行首展示实心完成状态。
- 节点排序：按住节点行上下拖动（与任务排序一致），松手后顺序持久化；未手动排序的节点按计划日期升序排列，无计划日期节点排在最后。
- 任务无节点时不展示“暂无节点”等提示文案。
- 已归档任务不可添加、编辑或删除节点。

### 2.7 历史任务列表

- 历史任务状态文案统一为“已完成 · 归档时间 / 未完成 · 归档时间”。
- 历史操作：撤销完成（询问是否同时撤销归档）、补记已完成、撤销归档（仅未完成且未过期的任务）、删除。

## 3. 进度维护与生命周期

- 在任务详情中可新增进度记录，单条最多 240 字；新增进度时可自定义记录时间，时间默认为不指定（未指定时仅按日期记录，生命周期中不展示具体时间）。
- 进度记录适用于当前进行中的任务；已归档任务不可新增进度。
- 每条进度记录显示在生命周期列表中，包含记录时间与内容；生命周期按维护的时间排序，未指定具体时间（仅日期）的事件排在当天有具体时间的事件之后。
- 支持删除单条历史进度记录，删除前显示应用内确认弹窗。
- 任务详情默认展示任务状态，不展开内容与进度编辑；点击“任务编辑”“添加进度”按钮后展开为可编辑状态（展开时隐藏下方生命周期栏与节点区）。
- 完成任务时可选择完成时间（列表画圈与详情页“已完成”均弹出完成时间确认），完成时间默认为不指定（未指定时仅按日期记录）；周期任务标记完成无需确认完成时间，直接标记完成。
- 生命周期自动记录：
  - 创建任务
  - 标题变更
  - 截止时间变更（历史数据兼容）
  - 紧急标记变更
  - 任务开始执行时间变更
  - 任务开始执行（未来任务到达开始时间或点击“进行中”时记录）
  - 挂起任务、激活任务
  - 新增节点、节点改名、节点计划时间变更
  - 完成节点（以进度记录形式）
  - 进度新增
  - 完成、归档（区分手工归档与系统隔天自动归档）、撤销完成、撤销归档
  - 删除任务
- 普通生命周期记录不可删除，仅进度记录支持删除。

## 4. 用户管理

- 支持创建多个本地用户，用户名最长 40 字。
- 支持切换当前用户；不同用户的任务与生命周期记录相互隔离。
- 支持重命名用户，并同步迁移该用户的所有任务数据。
- 支持删除用户；删除时会删除其待办、历史任务和生命周期记录。
- 删除用户、节点、进度记录等不可恢复的危险操作使用应用内确认弹窗，不使用系统原生浏览器确认框；非破坏性错误反馈当前使用浏览器原生提示框。

## 9. 自动化测试

- 使用 Vitest、Vue Test Utils 与 jsdom 覆盖前端核心交互；Rust 侧使用 `cargo check` 验证编译。
- 自动化测试分为“本次变更回归”与“通用主场景还原”两类；具体用例归属及执行策略见 `docs/TESTING_STRATEGY.md`。
- 执行 `npm test` 运行全量回归；`npm run test:watch` 进入监听模式。

## 5. 桌面与窗口设置

- 支持便签大小：小、中、大。
- 支持便签主题：黄色、蓝色、绿色、粉红、白色。
- 支持便签位置：居中、右上、右下、左上、左下。
- 支持开机自启动开关，使用当前 Windows 用户注册表启动项。
- 支持极简模式：
  - 隐藏系统窗口外框；
  - 隐藏任务栏入口；
  - 移除便签外部留白、边框和阴影。
- 非极简模式保留普通窗口外框及白色外沿。
- 主题、大小、位置、极简模式与当前用户选择均保存于本机。

## 6. 产品信息与隐私

- 产品简介展示产品名称、当前版本、开发者、数据说明、源码仓库、隐私政策与联系方式。
- 数据说明明确：便签数据默认保存在本机，不上传至服务器。
- 提供 GitHub 源码仓库和隐私政策链接。

## 7. 技术与数据

- 前端：Vue 3 + Vite。
- 桌面端：Tauri 2。
- 本地数据库：SQLite。
- 数据表：
  - `tasks`：任务主体、所属用户、时间、紧急状态、完成/归档/删除/挂起/开始执行状态。
  - `task_events`：任务生命周期与进度事件。
  - `task_milestones`：任务节点（名称、计划时间、完成时间）。
- 启动异常会写入 `%LOCALAPPDATA%\DesktopStickyNote\startup.log`，用于排查启动失败。

## 8. 当前不包含

- 云同步、账号登录、跨设备数据共享。
- 系统通知或截止时间提醒。
- 任务分类、标签、重复任务、附件。
- 多设备备份与恢复。
