# 桌面便签 / StickyNote - 已实现功能需求文档

## 1. 产品定位

面向 Windows 桌面的轻量本地待办便签工具。用户可在桌面上维护多个用户的待办事项，记录截止时间、紧急标记与执行进度。

- 当前应用展示版本：`v1.4.0 - 2026-08-31 15:21`
- 数据默认保存在本机 SQLite 数据库，不上传服务器。
- 支持 Microsoft Store MSIX 与便携 EXE 分发。

## 2. 任务管理

### 2.1 任务状态模型

任务状态分为三大类：

- 当前任务：进行中、未开始（未到开始执行时间）、已挂起、周期任务（由周期任务规则创建）。
- 已归档任务：已完成、未完成。
- 已删除任务（软删除，从历史任务中删除后进入）。

状态由 `completed_at / archived_at / deleted_at / suspended_at / start_at / recurring_setting_id` 字段派生，并在任务数据中随接口返回。

### 2.2 待办列表

- 展示当前用户的未归档任务（含未开始、已挂起、已完成未归档任务）。
- 任务行展示状态标签：进行中、未开始、已挂起、周期任务、已完成。
- 已挂起任务样式为灰色；已完成任务保留在当前列表并以划线方式展示，待手工归档或系统隔天自动归档后移入历史任务列表。
- 任务行操作改为收起的操作入口（“…”按钮），提供：挂起（进行中任务）、激活（已挂起任务）、已完成、归档。
- 排序规则：手工排序优先；其次紧急任务优先；随后按最早未完成节点计划时间升序；无节点任务靠后。
- 支持鼠标按住任务行拖拽进行手工排序，拖拽后顺序持久化保存。
- 任务卡展示任务标题、状态标签、下一节点信息与紧急标识；未来任务展示开始执行时间。
- 下一节点计划时间已过时，展示“已延迟”标识。
- 空列表时提示用户添加首个任务。

### 2.3 创建任务

- 支持填写任务标题，最大 80 字。
- 支持设置可选紧急标记。
- 支持勾选“未来任务”标签：自动添加“任务开始执行”节点，可维护任务开始执行时间；未到达开始执行时间时任务为“未开始”，到达后自动变更为“进行中”。
- 支持可选填写首个节点名称（最大 40 字）与计划时间，作为任务的首个节点。
- 标题为空时不可提交。
- 创建任务时自动写入生命周期记录。

### 2.4 编辑与完成

- 支持编辑任务标题、紧急标记与未来任务开始执行时间。
- 对任务内容、紧急状态、开始执行时间的每次实际变更均会写入生命周期记录。
- 支持完成任务：完成后任务保留在当前任务列表（划线展示），不立即归档；系统隔天自动归档，或由用户手工归档。
- 支持撤销完成：当前列表与历史列表均可撤销，任务恢复为待办/未完成状态。
- 已挂起任务可通过“激活”恢复为进行中。

### 2.6 周期任务

- 创建任务时可选择“一次性任务”或“周期任务”。
- 周期任务可配置长期或指定日期范围、每日/每工作日/指定每周几，以及每日生成时间（默认 06:00）。
- 系统在应用运行时自动补齐应生成的当日任务，名称附带任务日期；任务实例支持节点和进度维护。
- 跨日未完成实例自动归档为“未完成”，历史任务中可切换完成状态；仅当日实例撤销完成后会回到待办。
- 菜单提供“周期任务设置”，可查看状态、编辑后续生成规则和查看该设置已创建的任务；更新不追溯历史实例。

### 2.5 节点管理

- 任务支持维护多个节点，节点由名称和计划时间组成。
- 支持添加节点；节点名称必填（最大 40 字），计划时间可选。
- 支持编辑节点名称与计划时间；已完成的节点不可编辑。
- 支持标记节点完成，完成后记录完成时间，并自动生成一条进度记录。
- 已归档任务不可添加、编辑或完成节点。
- 节点按计划时间升序排列；无计划时间节点排在最后。

## 3. 进度维护与生命周期

- 在任务详情中可新增进度记录，单条最多 240 字。
- 进度记录适用于当前进行中的任务；已归档任务不可新增进度。
- 每条进度记录显示在生命周期列表中，包含记录时间与内容。
- 支持删除单条历史进度记录，删除前显示应用内确认弹窗。
- 任务详情默认展示任务状态，不展开内容与进度编辑；点击“编辑”“进度”按钮后展开为可编辑状态。
- 生命周期自动记录：
  - 创建任务
  - 标题变更
  - 截止时间变更（历史数据兼容）
  - 紧急标记变更
  - 任务开始执行时间变更
  - 任务开始执行（未来任务到达开始时间自动记录）
  - 挂起任务、激活任务
  - 新增节点、节点改名、节点计划时间变更
  - 完成节点（以进度记录形式）
  - 进度新增
  - 完成、归档（区分手工归档与系统隔天自动归档）、撤销完成、撤销归档
  - 删除任务
- 普通生命周期记录不可删除，仅进度记录支持删除。

## 4. 用户管理

- 支持创建多个本地用户，用户名最长 40 字。
- 支持切换当前用户；不同用户的任务与生命周期记录相互隔离。
- 支持重命名用户，并同步迁移该用户的所有任务数据。
- 支持删除用户；删除时会删除其待办、历史任务和生命周期记录。
- 删除用户、节点、进度记录等不可恢复的危险操作使用应用内确认弹窗，不使用系统原生浏览器确认框；非破坏性错误反馈当前使用浏览器原生提示框。

## 9. 自动化测试

- 使用 Vitest、Vue Test Utils 与 jsdom 覆盖前端核心交互；Rust 单元测试使用内存 SQLite 验证业务逻辑。
- 自动化测试分为“本次变更回归”与“通用主场景还原”两类；具体用例归属及执行策略见 `docs/TESTING_STRATEGY.md`。
- 执行 `npm run test:main` 运行通用主场景；执行对应 `test:change:*` 命令运行本次变更回归；`npm test` 执行全量回归；`npm run test:watch` 进入监听模式。

## 5. 桌面与窗口设置

- 支持便签大小：小、中、大。
- 支持便签主题：黄色、蓝色、绿色、粉红、白色。
- 支持便签位置：居中、右上、右下、左上、左下。
- 支持开机自启动开关，使用当前 Windows 用户注册表启动项。
- 支持极简模式：
  - 隐藏系统窗口外框；
  - 隐藏任务栏入口；
  - 移除便签外部留白、边框和阴影。
- 非极简模式保留普通窗口外框及白色外沿。
- 主题、大小、位置、极简模式与当前用户选择均保存于本机。

## 6. 产品信息与隐私

- 产品简介展示产品名称、当前版本、开发者、数据说明、源码仓库、隐私政策与联系方式。
- 数据说明明确：便签数据默认保存在本机，不上传至服务器。
- 提供 GitHub 源码仓库和隐私政策链接。

## 7. 技术与数据

- 前端：Vue 3 + Vite。
- 桌面端：Tauri 2。
- 本地数据库：SQLite。
- 数据表：
  - `tasks`：任务主体、所属用户、时间、紧急状态、完成/归档状态。
  - `task_events`：任务生命周期与进度事件。
  - `task_milestones`：任务节点（名称、计划时间、完成时间）。
- 启动异常会写入 `%LOCALAPPDATA%\DesktopStickyNote\startup.log`，用于排查启动失败。

## 8. 当前不包含

- 云同步、账号登录、跨设备数据共享。
- 系统通知或截止时间提醒。
- 任务删除功能。
- 任务分类、标签、重复任务、附件。
- 多设备备份与恢复。
